<%= form_with(model: image_file, local: true) do |form| %>
  <% if image_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_file.errors.count, "error") %> prohibited this image_file from being saved:</h2>

      <ul>
      <% image_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner %>
    <%= form.text_field :owner, required: true %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, required: true %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, required: true, accept: 'image/png,image/gif,image/jpeg' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
